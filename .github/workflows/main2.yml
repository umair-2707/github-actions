name: pull request

on:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  run_python_script:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Notification for creation of PR..
      run: |
        echo "Some of the pull requests have been closed"
